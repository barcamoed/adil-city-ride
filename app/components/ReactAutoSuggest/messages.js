/*
 * ReactAutoSuggest Messages
 *
 * This contains all the text for the ReactAutoSuggest component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReactAutoSuggest';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReactAutoSuggest component!',
  },
});

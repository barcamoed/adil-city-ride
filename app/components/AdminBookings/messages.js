/*
 * AdminBookings Messages
 *
 * This contains all the text for the AdminBookings component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AdminBookings';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminBookings component!',
  },
});

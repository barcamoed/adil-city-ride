/*
 * ReferralStats Messages
 *
 * This contains all the text for the ReferralStats component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReferralStats';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReferralStats component!',
  },
});

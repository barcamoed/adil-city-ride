/*
 * ReferalLogin Messages
 *
 * This contains all the text for the ReferalLogin component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReferalLogin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReferalLogin component!',
  },
});

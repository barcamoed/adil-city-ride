/*
 * AvailableCars Messages
 *
 * This contains all the text for the AvailableCars component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AvailableCars';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AvailableCars component!',
  },
});

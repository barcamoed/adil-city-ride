/*
 * ChooseDestination Messages
 *
 * This contains all the text for the ChooseDestination component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ChooseDestination';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChooseDestination component!',
  },
});

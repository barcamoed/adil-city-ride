/*
 * ReactGMap Messages
 *
 * This contains all the text for the ReactGMap component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReactGMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReactGMap component!',
  },
});

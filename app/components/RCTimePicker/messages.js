/*
 * TimePicker Messages
 *
 * This contains all the text for the TimePicker component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TimePicker';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TimePicker component!',
  },
});
